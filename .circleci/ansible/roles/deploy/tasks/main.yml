---

- name : "create backend dir"
  files:
    path: ~/backend-app
    state: directory

- name: "unarchive backend files"
  unarchive:
    src: artifact.tar.gz
    dist: ~/backend-app 

- name: "install dependencies" 
  shell: |
    cd ~/backend-app
    npm i

- name: "execute the backend"
  shell: | 
    cd ~/backend-app/dist
    pm2 stop default
    pm2 start main.js
 